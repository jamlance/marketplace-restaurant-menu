# marketplace-restaurant-menu

Deploy repo for the **restaurant-menu** Inkress-embedded app. Generated from `jamlance/bookerva-apps` — edit there, then re-run `scripts/sync-app-repo.mjs restaurant-menu --push`.

Coolify: base_directory `/`, dockerfile `/apps/restaurant-menu/Dockerfile`.
